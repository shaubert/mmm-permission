apply plugin: 'com.android.library'

dependencies {
    implementation 'com.shaubert.lifecycle.objects:library:1.5'
}

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 28
    }
}

apply from: '../maven_push.gradle'